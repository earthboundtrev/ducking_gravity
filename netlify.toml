[build]
  command = "node inject-env.js"
  publish = "."

# Netlify automatically uses 404.html for 404 errors
# This catch-all is for SPA routing - it only applies if no file matches
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Prefer Site settings → Environment variables in Netlify UI so values are not committed.
# These keys match what inject-env.js / dotenv expect during the build.
[build.environment]
  EMAILJS_PUBLIC_KEY = ""
  EMAILJS_SERVICE_ID = ""
  EMAILJS_TEMPLATE_ID = ""